<UserControl x:Class="NetworkService.Views.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:NetworkService.Controls"
             xmlns:viewmodel="clr-namespace:NetworkService.ViewModel"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodel:GraphViewModel, IsDesignTimeCreatable=False}">

    <Grid Background="{StaticResource Clr.Bg}" Margin="16">
        <Border Style="{StaticResource Card}" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource Txt.Title}" FontSize="16" Text="Measurement Graph"/>
                    <TextBlock Text="   •   " Foreground="{StaticResource Clr.SubText}" VerticalAlignment="Center"/>
                    <TextBlock Text="Entity:" Style="{StaticResource Txt.Label}" Margin="0,0,8,0"/>
                    <ComboBox Width="180"
                              ItemsSource="{Binding Entities}"
                              SelectedItem="{Binding SelectedEntity, Mode=TwoWay}"
                              DisplayMemberPath="Name"/>
                </StackPanel>

                <controls:BarChart Grid.Row="2"
                                   ItemsSource="{Binding Points}"
                                   Low="250"
                                   High="350"
                                   ValidBrush="#2E8B57"
                                   InvalidBrush="#B22222"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
